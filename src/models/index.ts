export type SessionLead = {
    id?: number;
    name: string;
};

export type Student = {
    id?: number;
    name: string;
};

export type Session = {
    id?: number;
    title: string;
    session_date: Date;
};
